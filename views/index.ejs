<!DOCTYPE html>
<html>
  <%- include('partials/head', {title}) %>
  <body>
    <%- include('partials/nav', {user}) %>
    <% if (user) { %>
      <h1>Welcome, <%= user.username %></h1>
    <% } else { %>
      <h1><%= title %></h1>
      <p>Welcome to <%= title %></p>
    <% } %>
  </body>
</html>
